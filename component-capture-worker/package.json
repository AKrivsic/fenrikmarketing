{
  "name": "fenrik-component-capture-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types server.ts",
    "check": "node --experimental-strip-types scripts/check-worker.ts && node --experimental-strip-types scripts/check-capture-selection.ts",
    "check:live": "node --experimental-strip-types scripts/check-live.ts",
    "check:capture-selection": "node --experimental-strip-types scripts/check-capture-selection.ts"
  },
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20",
    "esbuild": "^0.28.1"
  }
}
