{
  "name": "fenrik-component-capture-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types server.ts",
    "check": "node --experimental-strip-types scripts/check-worker.ts",
    "check:live": "node --experimental-strip-types scripts/check-live.ts"
  },
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20"
  }
}
