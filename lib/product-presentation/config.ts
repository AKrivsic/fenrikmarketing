/**
 * Product Presentation Decision (PPD) — sole presentation runtime.
 * Legacy PRODUCT_DEMO emit/require paths were removed in final cleanup.
 */
export function isProductPresentationDecisionEnabled(): boolean {
  return true;
}
